# react-freeze-dry

Hello! This is a tiny thing that lets you compose pages with React, and save them as static HTML files. Everything is rendered at build time, React is never loaded on the client.

All the fun is inside of `index.js`, the rest is just boilerplate around it.
